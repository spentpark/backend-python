[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "backend-python"
version = "1.0.0" # Se sobreescribe en el pipeline
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "aiomysql"
]